{
  "name": "@midnight-survey/contracts",
  "version": "1.0.0",
  "description": "Compact smart contracts for Midnight Survey DApp",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "true",
    "typecheck": "tsc --noEmit",
    "test": "true"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
